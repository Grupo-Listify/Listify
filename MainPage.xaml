<?xml version="1.0" encoding="utf-8" ?>
<!--
  MainPage.xaml
  Esta es la pagina de ejemplo que viene con la plantilla de MAUI.
  No esta registrada en AppShell, por lo que no se muestra en la navegacion principal.
  Puedes dejarla para pruebas o eliminarla si no la usas.

  Notas rapidas:
  - Code-behind: MainPage.xaml.cs (maneja el evento OnCounterClicked).
  - Estilos usados (Headline, SubHeadline) se cargan desde Resources/Styles/Styles.xaml.
  - La imagen dotnet_bot.png debe existir en Resources/Images (propiedad Build Action: MauiImage).
-->

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Listify.MainPage"
             Title="MainPage (ejemplo)">

    <!-- ScrollView permite que el contenido se desplace si no cabe en pantalla -->
    <ScrollView>

        <!-- Contenedor vertical con separacion y padding basico -->
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <!-- Imagen de ejemplo incluida en la plantilla -->
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot en un auto de carrera numero ocho" />

            <!-- Titulo grande. Usa un estilo definido en los recursos -->
            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <!-- Subtitulo de muestra con salto de linea -->
            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Bienvenido a dot net multi plataforma app u i" />

            <!-- Boton de contador de clicks de ejemplo.
                 El evento OnCounterClicked esta en MainPage.xaml.cs -->
            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Cuenta cuantas veces haces click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
